---
import { EMAIL, GITHUB_URL, LINKEDIN_URL, PHONE_NUMBER, WHATSAPP_URL } from "../../constants/global";
import EmailIcon from "../icons/EmailIcon.astro";
import GitHubIcon from "../icons/GitHubIcon.astro";
import LinkedInIcon from "../icons/LinkedInIcon.astro";
import WhatsAppIcon from "../icons/WhatsAppIcon.astro";
import LinkButton from "../LinkButton.astro";

interface Props {
  title: string;
  subtitle: string;
}

const { title, subtitle } = Astro.props
---

<section class="max-w-6xl mx-auto py-30 md:py-35 px-4 md:px-0" id="Contact">
  <div class="text-center mb-6">
    <h2 class="text-3xl font-bold text-white">{title}</h2>
    <p class="text-white">{subtitle}</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-5 md:gap-2 lg:gap-4 max-w-6xl mx-auto px-4">
    <LinkButton href={LINKEDIN_URL} title="LinkedIn" target="_blank">
        <LinkedInIcon slot="before" />
            <diV class="flex flex-col">
                LinkedIn
                <p slot="after" class="text-sm text-amber-100">/in/samuu-crbnll</p>
            </diV>
        </LinkButton>
        <LinkButton href={`mailto:${EMAIL}`} title="Email">
            <EmailIcon slot="before" />
            <diV class="flex flex-col">
                Email
                <p slot="after" class="text-sm text-amber-100">{EMAIL}</p>
            </diV>
        </LinkButton>
        <LinkButton href={GITHUB_URL} title="Github" target="_blank">
            <GitHubIcon slot="before" />
            <diV class="flex flex-col">
                Github
                <p slot="after" class="text-sm text-amber-100">/SCrbnll</p>
            </diV>
        </LinkButton>
        <LinkButton href={WHATSAPP_URL} title="WhatsApp" target="_blank">
            <WhatsAppIcon slot="before"/>
            <diV class="flex flex-col">
                WhatsApp
                <p slot="after" class="text-sm text-amber-100">{PHONE_NUMBER}</p>
            </diV>
        </LinkButton>
  </div>
</section>
